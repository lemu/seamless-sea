# TODO

## Pending Tasks

- release and upgrade new tide-ui

- remove emojis from load port/ discharge port in overview section in /fixtures sidebar, replace them with two letter code, eg. Hamburg, DE

- Merge the owner x charterer section from the sidebar title with the line below, adjust the sizes of avatars and font so it matches

- make sure pagination setting is saved to local storage

- When i change the page size, we should show loading table skeleton, because it freezes for a bit, or should

- pass bookmark/filtering parameters via url parameters?
